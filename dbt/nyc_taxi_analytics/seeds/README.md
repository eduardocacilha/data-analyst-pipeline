# Seeds

`taxi_zone_lookup.csv` — tabela de referência das zonas de táxi de Nova York
(usada para virar `dim_zone` na camada gold).

**Este arquivo aqui é só um exemplo com poucas linhas** para o dbt já rodar
sem erro. Na Semana 1, baixe o arquivo oficial completo (265 zonas) em:

https://d37ci6vzurychx.cloudfront.net/misc/taxi_zone_lookup.csv

e substitua este CSV pelo conteúdo real antes de rodar `dbt seed`.
